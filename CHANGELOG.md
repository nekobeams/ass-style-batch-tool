# Changelog

本檔案記錄每個版本的重要變更。標題格式固定為 `## v<版本號>`(需與
git tag 完全一致),`.github/workflows/release.yml` 發版時會自動擷取
對應段落當作 GitHub Release notes(見 `.github/scripts/extract_changelog_section.py`
與 `CLAUDE.md`「發版流程」一節)。

## v1.1.1

2026-08-05

第一個透過全自動 Release 工作流(`.github/workflows/release.yml`)發布
的版本,本身沒有應用程式層面的功能變化。

- 新增:打 `v*` tag 後自動跑測試 → PyInstaller 打包 → Inno Setup 編譯
  安裝程式 → 建立 GitHub Release 並上傳安裝檔,不必再手動跑這些步驟
- 測試沒過就不會發布(硬性條件)
- tag 版本號與 `VERSION` 檔不一致時會直接失敗,避免發錯版本
- libmpv-2.dll 改由專案自己代管的 vendor release 資產下載,不必手動
  放進打包環境
- 已知限制:CI 產出的安裝程式只含 core 元件,不含 ffmpeg/MKVToolNix
  選配安裝(需要完整版本時仍要照 `CLAUDE.md` 的手動打包步驟做)

## v1.1.0

2026-08-05

**新功能:程式現在會留下日誌,方便回報問題。**

- 主視窗新增「開啟日誌資料夾」按鈕
- 日誌檔位置:`%LOCALAPPDATA%\ass-style-batch-tool\logs\app.log`(附
  輪替檔 `app.log.1`/`.2`/`.3`)
- 修掉幾處之前遇到錯誤會被靜默忽略、完全沒有任何紀錄的地方
- 內部程式碼整理(不影響操作方式)

## v1.0.2

2026-08-05

維護版本,沒有使用者看得到的功能變更,程式行為與 v1.0.1 完全相同。

- 補上標準的 Python 套件打包設定(`pyproject.toml`),方便從原始碼安裝
- 補強測試覆蓋率,鎖定幾個先前沒有測試驗證過的邊界情況

## v1.0.1

2026-08-04

修正版本,建議所有 v1.0.0 使用者更新。

- **修掉一個會讓程式當掉的問題。** 三個分頁(字幕檔、MKV、封裝)共 9
  處背景執行緒的釋放時機交給 Python 記憶體回收機制、時機不可控,運氣
  不好會讓程式無預警消失。已全部修正,改由 Qt 自己掌控釋放時機
- 新增 12 個回歸測試,直接檢查背景工作物件在收尾後確實被釋放
- 加入自動化測試流程(GitHub Actions CI)
- 文件補上四個分頁的使用說明與畫面截圖,新增簡短英文說明

## v1.0.0

2026-08-03

初始版本。批次修改整季動畫字幕的樣式,依片源解析度自動換算數值、只改
指定的樣式、依集數編號配對字幕與影片。

- **字幕檔**——批次套用樣式或縮放字級,掃描後即時預覽每一集會被改成
  什麼
- **MKV**——直接修改封裝進 MKV 容器裡的字幕軌
- **封裝**——把外部字幕檔(可選是否先套用樣式)封裝進對應的 MKV
- **樣式與預覽**——內嵌 mpv,點字幕行跳轉播放位置,即時看到樣式效果
